TOP := $(dir $(abspath $(lastword $(MAKEFILE_LIST))))
GFLAGS ?=
JFLAGS ?=
JASMINC ?= jasminc

%_export.japp: %.jazz
	gpp -I../ $(GFLAGS) -DEXPORT -o $@ $<

%.japp: %.jazz
	gpp -I. -I$(TOP) $(GFLAGS) -o $@ $<

%.s: %.japp
	$(JASMINC) $(JFLAGS) -pasm $< > $@ || rm -f $@

clean:
	@rm -f *.japp *.s
