#include "x25519_tobytes.jazz"

export fn fe64_tobytes(reg u64 _h _f)
{
  inline int i;
  reg u64[4] h f;

  for i=0 to 4
  { f[i] = [_f + 8*i]; }

  h = _fe64_tobytes(f);

  for i=0 to 4
  { [_h + 8*i] = h[i]; }
}
