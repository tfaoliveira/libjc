#include "x25519_sqr_regs.jazz"

export fn fe64_sqr_regs(reg u64 _h _f)
{
  inline int i;
  stack u64 _hs;
  reg u64[4] h f;

  _hs = _h;

  for i=0 to 4
  { f[i] = [_f + 8*i]; }

  h = _fe64_sqr_regs(f);

  _h = _hs;
  for i=0 to 4
  { [_h + 8*i] = h[i]; }
}
