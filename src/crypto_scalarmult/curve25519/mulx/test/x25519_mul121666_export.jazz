#include "x25519_mul121666.jazz"

export fn fe64_mul121666(reg u64 _h _f)
{
  inline int i;
  stack u64[4] f;
  reg u64[4] h;
  reg u64 t;

  for i=0 to 4
  { t = [_f + 8*i];
    f[i] = t;
  }

  h = _fe64_mul121666(f);

  for i=0 to 4
  { [_h + 8*i] = h[i]; }
}
